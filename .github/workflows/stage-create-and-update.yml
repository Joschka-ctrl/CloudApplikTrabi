name: Production Cluster Creation

on:
  push:
    tags:
      - '0.*' 

jobs:
  create-Stage-free:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install GitHub CLI
        run: |
          sudo apt install gh -y

      - name: Trigger another pipeline
        run: |
          gh workflow run cluster-create-k8s.yml -f tenant_name=stage-free -R Joschka-ctrl/CloudApplikTrabi -r stage-cluster
        env:
          GH_TOKEN: ${{ secrets.TOKEN }}  

  create-Stage-standard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install GitHub CLI
        run: |
          sudo apt install gh -y

      - name: Trigger another pipeline
        run: |
          gh workflow run cluster-create-k8s.yml -f tenant_name=stage-parking -R Joschka-ctrl/CloudApplikTrabi -r stage-cluster
        env:
          GH_TOKEN: ${{ secrets.TOKEN }}