apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-backend-reporting
spec:
  type: {{ .Values.backend-reporting.service.type }}
  ports:
    - port: {{ .Values.backend-reporting.service.port }}
      targetPort: {{ .Values.backend-reporting.service.targetPort }}
      protocol: TCP
  selector:
    app: backend-reporting
